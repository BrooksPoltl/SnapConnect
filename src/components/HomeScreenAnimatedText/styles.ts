/**
 * HomeScreenAnimatedText Component Styles
 *
 * Minimal styles for the HomeScreen-specific animated text component.
 * Most styling should be passed via props to maintain flexibility.
 */

import { StyleSheet } from 'react-native';
import { Theme } from '../../types/theme';

export const styles = (_theme: Theme) =>
  StyleSheet.create({
    // Base container style if needed for HomeScreen-specific animations
    container: {
      // Add any HomeScreen-specific base styles here if needed in the future
    },
  });
